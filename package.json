{
  "scripts": {
    "build": "web-ext build --source-dir src",
    "lint": "web-ext lint --source-dir src"
  },
  "devDependencies": {
    "standard": "^16.0.1",
    "web-ext": "^5.3.0"
  },
  "engines": {
    "yarn": "^1.22.5"
  }
}
